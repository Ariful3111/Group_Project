import 'package:flutter/material.dart';

class Bottom_Nav_controller extends StatelessWidget {
  const Bottom_Nav_controller({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
